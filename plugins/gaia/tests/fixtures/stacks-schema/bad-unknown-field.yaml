# Negative fixture — an unknown property on a stacks[] item. Because the
# items schema keeps additionalProperties:false, this MUST be REJECTED by
# ajv (guards against the additionalProperties:false-trap regression).
project_root: /tmp/gaia
project_path: /tmp/gaia/app
memory_path: /tmp/gaia/_memory
checkpoint_path: /tmp/gaia/_memory/checkpoints
installed_path: /tmp/gaia/_gaia
framework_version: 1.176.0
date: 2026-05-25
stacks:
  - name: api
    language: go
    paths: ["services/api/**"]
    typo_field: oops
