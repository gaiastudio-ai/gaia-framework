# Test fixture: project config with bridge_enabled=false.
# The skill must short-circuit with verdict=SKIPPED.
# AF-2026-05-17-10: platforms: declared so the new mobile-platforms gate passes.
platforms:
  - ios
  - android

test_execution_bridge:
  bridge_enabled: false
device_farm:
  adapter: firebase-test-lab
