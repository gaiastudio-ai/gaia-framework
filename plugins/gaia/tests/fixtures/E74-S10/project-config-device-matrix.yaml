# Test fixture: project config used by gaia-test-device-matrix dispatch tests.
test_execution_bridge:
  bridge_enabled: true
device_farm:
  adapter: firebase-test-lab
device_targets:
  os_versions: ["14"]
  form_factors: ["phone"]
  screen_sizes: ["default"]
