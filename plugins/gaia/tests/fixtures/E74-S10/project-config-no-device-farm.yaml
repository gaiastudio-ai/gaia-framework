# Test fixture: project config with no device-farm adapter configured.
# The skill must fail gracefully with verdict=ERROR.
# AF-2026-05-17-10: platforms: declared so the new mobile-platforms gate passes.
platforms:
  - ios
  - android

test_execution_bridge:
  bridge_enabled: true
