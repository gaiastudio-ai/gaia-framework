# Test fixture: project config with no mobile platform declared (AF-2026-05-17-10).
# Used to verify the platforms-mobile defense-in-depth gate fires SKIPPED.
platforms:
  - claude-plugin

test_execution_bridge:
  bridge_enabled: true

device_farm:
  adapter: firebase-test-lab
