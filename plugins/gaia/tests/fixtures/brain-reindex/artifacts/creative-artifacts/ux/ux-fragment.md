# UX fragment (reindex fixture)

A tiny UX artifact that references the primary story key so the harvester emits a
`designs` (LATERAL) edge for it. The query's governance envelope groups this
under LATERAL.

- Screen: Primary node detail, designed for E777-S2.
