{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gaia.studio/schemas/threat-model.schema.json",
  "title": "Threat model",
  "description": "Backing JSON Schema for the `threat-model` artifact type so /gaia-val-validate can enforce structural invariants, not merely route the type. The threat-model artifact is a prose-markdown document (STRIDE threat identification + DREAD risk scoring) with a YAML frontmatter metadata block (grounded in the on-disk corpus instance `.gaia/artifacts/planning-artifacts/threat-model.md`). The shared validate-artifact-schema.sh helper extracts the frontmatter for *.md instances and validates it against this schema; the prose body is not machine-validated by a frontmatter schema, so the nine canonical H2 sections it MUST carry are documented in `x-required-sections` below for the document-ruleset and reviewer. Note: `threat-model` is already present in the /gaia-val-validate artifact_type enum — this schema enforces structural invariants without any enum change. Pragmatic, grounded shape — required metadata is pinned, but additional frontmatter keys (e.g. `framework_version`, `methodology`, `author`) are permitted.",
  "type": "object",
  "required": ["template", "version", "date", "project"],
  "additionalProperties": true,
  "properties": {
    "template": {
      "type": "string",
      "description": "Template identifier. MUST be `threat-model` — the slug that selects the document-specific ruleset and aligns with the on-disk artifact filename per the /gaia-val-validate artifact_type enum.",
      "const": "threat-model"
    },
    "version": {
      "type": "string",
      "description": "Template/document version (semver-shaped, e.g. `1.0.0`). Bumped when the threat model is re-run under a newer template. Note: quote in YAML frontmatter so a value like `1.0` is not parsed as a float by the validate-artifact-schema.sh YAML->JSON bridge.",
      "minLength": 1
    },
    "date": {
      "type": "string",
      "description": "Threat-model date. ISO-8601 `YYYY-MM-DD`. Quote in YAML frontmatter (`date: \"2026-03-14\"`) so the validate-artifact-schema.sh YAML->JSON bridge does not load it as a non-serializable date object.",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
    },
    "project": {
      "type": "string",
      "description": "Human-readable project name the threat model covers (e.g. `GAIA Framework`).",
      "minLength": 1
    },
    "methodology": {
      "type": "string",
      "description": "Optional. The threat-modelling methodology, when recorded (e.g. `STRIDE + DREAD`). Informational provenance, present in the corpus instance header."
    },
    "author": {
      "type": "string",
      "description": "Optional. The authoring persona/agent, when recorded (e.g. `Zara (Application Security Expert)`). Informational provenance."
    },
    "framework_version": {
      "type": "string",
      "description": "Optional. The GAIA framework version at threat-model time, when recorded. Informational provenance."
    }
  },
  "x-required-sections": {
    "description": "The nine canonical H2 (`##`) sections the prose body MUST carry, grounded in the corpus instance + /gaia-threat-model Step structure. In the corpus instance these H2 headings are numbered (e.g. `## 1. Assets Inventory`); the section list below is the unnumbered canonical name. Validated by the document-ruleset / reviewer, not by this frontmatter schema.",
    "sections": [
      "Assets Inventory",
      "Trust Boundaries",
      "STRIDE Analysis",
      "DREAD Scoring",
      "Mitigation Strategies",
      "Security Requirements",
      "Risk Acceptance Register",
      "Threat Model Diagram",
      "Summary"
    ]
  }
}
