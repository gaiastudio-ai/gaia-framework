# deferral-phrases.txt — Closed-list deferral-phrase taxonomy (v1, frozen)
#
# Status: v1-frozen. Amendments MUST follow the amendment process
# (PRD record + cross-reference + bumped version + downstream review).
#
# Consuming skills (3, as of v1):
#   - gaia-dev-story Step 12.5 (deferral scan)
#   - gaia-fix-story (deferral classification when re-validating)
#   - gaia-val-validate (drift-prevention scan of plan and diff text)
#
# Lines starting with `#` are comments. Blank lines are ignored.
# Each non-comment line is one canonical deferral phrase, matched with
# `grep -wF` (fixed-string, word-boundary) by deferral-phrase-match.sh.
deferred
follow-up integration story
stub seam
harness wiring lands
not-yet-wired
production wiring
