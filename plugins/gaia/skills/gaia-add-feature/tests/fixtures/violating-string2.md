# Test fixture — violates string 2

This fixture intentionally contains the smoking-gun string for AF-4 / inline-Val:

**Step 2** — Val review gate (inline, read-only verification — Agent dispatch not surfaced).

It MUST trip the assessment-doc-bypass-check scanner.
