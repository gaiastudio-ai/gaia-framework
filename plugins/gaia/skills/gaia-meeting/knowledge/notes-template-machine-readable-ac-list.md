# Acceptance Criteria

> **Closing-artifact bias:** `machine-readable-ac-list` (mode: `ac`).
> Drafted at CLOSE — feeds the facilitator's notes-drafting prompt.

The Given/When/Then bullets below are intentionally formatted to feed
`/gaia-edit-test-plan` or a story file as-is — copy the body directly.

## AC list

- **AC1:** Given <preconditions>, when <action>, then <observable outcome>.
- **AC2:** Given <preconditions>, when <action>, then <observable outcome>.
- **AC3:** Given <preconditions>, when <action>, then <observable outcome>.

## Trace

- AC1 → FR-/NFR-/TC- citations (one per AC).
- AC2 → …
- AC3 → …

## Follow-ups

- `ac-followup` action items for any AC that needs validation against the
  PRD or test plan before merge.
