# Architecture Decisions

> **Closing-artifact bias:** `architecture-decisions` (mode: `architecture`).
> Drafted at CLOSE — feeds the facilitator's notes-drafting prompt.

Each decision below uses an ADR-style record. Convert to a real ADR via
`/gaia-edit-arch` (if the architecture doc already exists) or
`/gaia-create-arch` (greenfield).

## ADR-XXX — <decision title>

- **Status:** proposed | accepted | superseded.
- **Context:** Why is this decision needed? What forces are at play?
- **Decision:** State the decision in one sentence.
- **Alternatives considered:**
  - Option A — short rationale.
  - Option B — short rationale.
- **Consequences:**
  - Positive: …
  - Negative: …
  - Follow-up `arch-edit` action item: …

## ADR-XXX — <next decision>

(Repeat the structure above per decision surfaced.)

## Follow-ups

- `arch-edit` action items feeding `/gaia-edit-arch` (or `/gaia-create-arch`
  for greenfield architecture documents).
