{
  "name": "yamllint",
  "provider": "yamllint",
  "category": "linter",
  "version-range": ">=1.32.0",
  "runtime-profile": "subprocess",
  "default-timeout-seconds": 60,
  "file-extensions": [".yaml", ".yml"],
  "scope": "file",
  "description": "YAML linter adapter. Wraps `yamllint` to validate YAML syntax, indentation, and style conventions. Five-file layout (adapter.json + probe.sh + run.sh + normalize.sh + test/contract.bats)."
}
