adapter_name: publish-homebrew
adapter_version: "1.0.0"
channel: homebrew
verify_retry_window_seconds: 600
credential_env_vars:
  - HOMEBREW_GITHUB_TOKEN
description: Publish a Homebrew formula update via tap PR; verify by polling the tap. Long propagation window (600s).
