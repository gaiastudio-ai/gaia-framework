# Generated CI-scoped config slice — DO NOT EDIT BY HAND.
# Source: the canonical project-config.yaml (kept outside / gitignored within this repo).
# Regenerate: plugins/gaia/scripts/gen-ci-config.sh --config <canonical> --out .gaia/ci-config.yaml
# Carries ONLY CI-matrix inputs (stacks + ci_cd.promotion_chain + test_policy) —
# no secrets, no local paths, no environments/release/distribution. A CI lint
# regenerates this and fails on drift.
stacks:
  - name: gaia-core
    language: bash
    paths:
      - plugins/gaia/scripts/**
      - plugins/gaia/skills/**
      - plugins/gaia/agents/**
      - plugins/gaia/hooks/**
      - plugins/gaia/tools/**
      - plugins/gaia/config/**
      - plugins/gaia/schemas/**
      - plugins/gaia/rubrics/**
      - plugins/gaia/templates/**
      - plugins/gaia/knowledge/**
      - plugins/gaia/docs/**
      - plugins/gaia/tests/**
      - plugins/gaia/test/**
    test_cmd: "bash plugins/gaia/tests/run-with-coverage.sh"
  - name: gaia-docs
    language: bash
    paths:
      - documentation/**
    test_cmd: "bats plugins/gaia/tests/af-2026-05-28-2-test07-docs.bats plugins/gaia/tests/af-2026-06-01-1-test15-findings.bats plugins/gaia/tests/af-2026-06-02-1-test16-findings.bats plugins/gaia/tests/af-2026-06-03-2-tc-osv-trace.bats plugins/gaia/tests/forbidden-sentinel-scan.bats plugins/gaia/tests/issue-1395-brownfield-walkthrough-tier2.bats plugins/gaia/tests/mode-b-docs.bats"
  - name: gaia-ci
    language: bash
    paths:
      - .github/**
    test_cmd: "bash .github/scripts/structure-validate.sh && bash .github/scripts/lint-skill-frontmatter.sh && bash .github/scripts/lint-agent-frontmatter.sh"
ci_cd:
  promotion_chain:
    - id: staging
      name: Staging
      branch: staging
      ci_provider: github_actions
      merge_strategy: squash
      ci_checks:
        - plugin-ci
        - selective-tests
    - id: main
      name: main
      branch: main
      ci_provider: github_actions
      merge_strategy: squash
      ci_checks:
        - plugin-ci
        - selective-tests
test_policy:
  retry_limit: 2
